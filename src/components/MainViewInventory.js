import React from 'react';
import ReactDOM from 'react-dom';
import Button from '@material-ui/core/Button';

const MainViewInventory = () => {
    return(
        <Button variant='contained' color="primary">
            Hello World
        </Button>
    )
}

export default MainViewInventory